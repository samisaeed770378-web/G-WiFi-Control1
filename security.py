def security_summary():
    return {
        "note": (
            "Security status is reported only when the connected router API "
            "provides verified information. V1 does not invent firewall/WPS/threat values."
        )
    }
